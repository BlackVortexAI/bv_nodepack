// Detached harness adapter for the application singleton only.
// Production registry/provider reconciliation and native graphs are unchanged.
export const app:any={graph:null,canvas:null};
export const api:any={};
